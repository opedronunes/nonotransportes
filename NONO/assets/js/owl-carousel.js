/**
 * CHAMADA DA FUNCTION
 */
$(document).ready(function(){
    $('.owl-carousel').owlCarousel();
    });
    
/**
 * CONFIG CAROUSEL
 */
$('.owl-carousel').owlCarousel({
    loop:true,
    margin:5,
    autoWidth:true,
    autoplay:true,
    autoplayTimeout:5000
})
